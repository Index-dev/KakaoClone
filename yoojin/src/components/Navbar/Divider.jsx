import React from 'react';
import './Divider.scss';

const Divider = ({dividerTitle})=>{
    return (
        <div className="divider">
            {dividerTitle}
        </div>
    )
}

export default Divider
